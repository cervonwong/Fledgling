# Gemini workspace files

This directory stores Gemini-local workspace settings for this repository.

- `settings.json` points Gemini tools to the project context file (`gemini.MD`)
- add custom MCP server definitions under `mcpServers` when needed
